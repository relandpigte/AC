import { DragClassUpdaterDirective } from './drag-class-updater.directive';

describe('DragClassUpdaterDirective', () => {
  it('should create an instance', () => {
    const directive = new DragClassUpdaterDirective();
    expect(directive).toBeTruthy();
  });
});

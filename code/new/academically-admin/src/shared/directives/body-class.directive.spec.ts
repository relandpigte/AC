import { BodyClassDirective } from './body-class.directive';

describe('BodyClassDirective', () => {
  it('should create an instance', () => {
    const directive = new BodyClassDirective();
    expect(directive).toBeTruthy();
  });
});

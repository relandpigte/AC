import { ToggableContentDirective } from './toggable-content.directive';

describe('ToggableContentDirective', () => {
  it('should create an instance', () => {
    const directive = new ToggableContentDirective();
    expect(directive).toBeTruthy();
  });
});

import { ImagePageComponent } from './image-page-component';

describe('ImagePageComponent', () => {
  it('should create an instance', () => {
    expect(new ImagePageComponent()).toBeTruthy();
  });
});

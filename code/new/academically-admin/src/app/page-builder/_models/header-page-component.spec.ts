import { HeaderPageComponent } from './header-page-component';

describe('HeaderPageComponent', () => {
  it('should create an instance', () => {
    expect(new HeaderPageComponent()).toBeTruthy();
  });
});

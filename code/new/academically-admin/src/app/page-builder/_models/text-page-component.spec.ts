import { TextPageComponent } from './text-page-component';

describe('TextPageComponent', () => {
  it('should create an instance', () => {
    expect(new TextPageComponent()).toBeTruthy();
  });
});

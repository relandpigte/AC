import { SidebarCollapseDirective } from './sidebar-collapse.directive';

describe('SidebarCollapseDirective', () => {
  it('should create an instance', () => {
    const directive = new SidebarCollapseDirective();
    expect(directive).toBeTruthy();
  });
});

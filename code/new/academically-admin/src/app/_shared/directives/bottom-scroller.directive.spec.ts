import { BottomScrollerDirective } from './bottom-scroller.directive';

describe('BottomScrollerDirective', () => {
  it('should create an instance', () => {
    const directive = new BottomScrollerDirective();
    expect(directive).toBeTruthy();
  });
});

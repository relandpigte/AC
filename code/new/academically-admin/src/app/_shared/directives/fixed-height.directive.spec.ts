import { FixedHeightDirective } from './fixed-height.directive';

describe('FixedHeightDirective', () => {
  it('should create an instance', () => {
    const directive = new FixedHeightDirective();
    expect(directive).toBeTruthy();
  });
});
